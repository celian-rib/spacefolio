version: '3'

services:
  spacefolio:
    container_name: spacefolio
    build:
      context: .
      dockerfile: Dockerfile
    ports:
            - '127.0.0.1:3000:3000'
    restart: unless-stopped
